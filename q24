#include<iostream>
using namespace std;
int main(){
   int n,i=1;
   cout<<"Enter the number\n";
   cin>>n;
   int fact=1;
   do
   {
      fact*=i;
      i++;
   }while(i<=n);
   cout<<"Factorial is"<<fact;
return 0;
}
