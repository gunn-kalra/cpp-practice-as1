#include<iostream>
using namespace std;
int main(){
   int n,flag=1;
   cout<<"Enter the number\n";
   cin>>n;
   int i=2;
   while(i<n)
   {
      if(n%i==0)
      flag=0;
      i++;
   }
   if(flag==0)
   cout<<"Number is not prime";
   else
   cout<<"Number is prime";
return 0;
}
